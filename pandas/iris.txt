IRIS DATASET (iris.csv)
📥 Load & Inspect
python
df = pd.read_csv('iris.csv')
# Columns: sepal_length, sepal_width, petal_length, petal_width, species
🟢 BEGINNER TASKS
Basic Stats

Calculate mean, median, std for each numeric column

Group by species and find average of each feature

Count number of samples per species

Find min and max values for each feature

Filtering

Select all setosa samples

Find flowers with petal_length > 4.0

Select samples where sepal_width < 3.0

Find the largest sepal_length for each species

Sorting & Ranking

Sort by petal_length descending

Find top 5 flowers with largest sepal_width

Rank flowers by petal_width within each species

🟡 INTERMEDIATE TASKS
Categorization

python
# Create size categories based on petal_length
df['Petal_Size'] = pd.cut(df['petal_length'], 
                           bins=[0, 2, 4, 10], 
                           labels=['Small', 'Medium', 'Large'])
Count species in each petal size category

Find species with largest average sepal length

Create sepal area column (sepal_length * sepal_width)

Statistical Analysis

Calculate correlation between all numeric features

Which two features are most correlated?

Find the species with most variation in petal_width (highest std)

Calculate IQR for each feature by species

Comparisons

python
# Find differences between species
df.groupby('species').agg({
    'sepal_length': ['mean', 'std', 'min', 'max']
})
Which species has the largest sepal length variability?

Find flowers that have petal_length > species average

Create a comparison table: mean values for all features by species

🔴 ADVANCED TASKS
Outlier Detection

python
# Identify outliers using IQR method
Q1 = df['sepal_length'].quantile(0.25)
Q3 = df['sepal_length'].quantile(0.75)
IQR = Q3 - Q1
outliers = df[(df['sepal_length'] < Q1 - 1.5*IQR) | 
              (df['sepal_length'] > Q3 + 1.5*IQR)]
Find outliers in each feature

Which species has the most outliers?

Remove outliers and recalculate means

Feature Engineering

Create Sepal_Ratio = sepal_length / sepal_width

Create Petal_Ratio = petal_length / petal_width

Create Size_Total = sepal_length + petal_length

Do these new features separate species better?

Complex Grouping

python
# Find species that overlap in feature space
df.groupby('species').agg({
    'petal_length': ['min', 'max'],
    'petal_width': ['min', 'max']
})
Which species have overlapping petal length ranges?

Find flowers that could be misclassified (feature values overlap)

Create "ambiguous" flag for overlapping regions

Pivot Tables

python
# Pivot: average features by species
df.pivot_table(index='species', 
               values=['sepal_length', 'petal_length'], 
               aggfunc='mean')
Create pivot table showing min, max, mean of all features by species

Compare setosa vs non-setosa (create binary column)

